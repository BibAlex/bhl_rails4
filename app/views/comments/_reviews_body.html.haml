.modal-dialog
  .modal-content
    .modal-header
      %button.close{ "aria-hidden" => "true", "data-dismiss" => "modal", "type" => "button" } ×
      %h4#myModalLabel.modal-title #{I18n.t('common.write_review')}
    .modal-body
      - if is_logged_in?
        = render partial: "comments/new_comment", locals: { commentable_id: params[:id], commentable_type: commentable_type }
      - else
        = link_to I18n.t('msgs.please_login'), login_users_path(locale: I18n.locale)